# Este é um modelo de controler que utiliza app/services/charts.rb para gerar um grafico 
#para uma aplicação de clinicas médicas que pode ser adaptado para outras necessidades.

#require 'charts'

#class ReportController < ApplicationController
  #def index

    #@total_appointments = Appointment.all
    #@appointments_returns = Appointment.where("category == '2'")

    #Charts.new(@total_appointments.size, @appointments_returns.size, 0, 0).gerar_grafico_consultas

  #end
#end
